apiVersion: v1
kind: Service
metadata:
  name: file-registry-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: file-registry
spec:
  type: ClusterIP
  ports:
    - port: 3007
      targetPort: 3007
      protocol: TCP
  selector:
    app: file-registry

apiVersion: v1
kind: Service
metadata:
  name: upload-session-svc
  namespace: {{ .Values.namespace }}
  labels:
    app: upload-session
spec:
  type: ClusterIP
  ports:
    - port: 3008
      targetPort: 3008
      protocol: TCP
  selector:
    app: upload-session

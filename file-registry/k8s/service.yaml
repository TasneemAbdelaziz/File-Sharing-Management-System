apiVersion: v1
kind: Service
metadata:
  name: file-registry
  namespace: file-platform
  labels:
    app: file-registry
spec:
  type: ClusterIP
  selector:
    app: file-registry
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3007

apiVersion: v1
kind: Service
metadata:
  name: upload-session
  namespace: file-platform
  labels:
    app: upload-session
spec:
  type: ClusterIP
  selector:
    app: upload-session
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3008
